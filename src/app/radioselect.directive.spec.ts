import { RadioselectDirective } from './radioselect.directive';

describe('RadioselectDirective', () => {
  it('should create an instance', () => {
    const directive = new RadioselectDirective();
    expect(directive).toBeTruthy();
  });
});
